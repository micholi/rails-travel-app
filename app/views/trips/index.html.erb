<h4 class="align-table"><%= @user.name %>'s Trips</h4>
<% @trips.each do |trip| %>
  <p><%= link_to trip.city.city_plus_country, user_trip_path(@user, trip) %></p>
  <div id="trip-<%= trip.id %>"><%= truncate(trip.comment) %></div>
  <a href="#" class="js-more" data-id="<%= trip.id %>">Read More</a>
  <ul id="trip-<%= trip.id %>-">
  </ul>
<% end %>

<script type="text/javascript" charset="utf-8">
$(function() {
  $(".js-more").on("click", function() {
    var id = $(this).data("id");
    $.get("trips/" + id + ".json", function(data) {
      var trip = data;
      var reviewText = "<p>" + trip["comment"] + "</p><p>" + "Rating: " + trip["rating"] + "</p><p>" + "Must See Attraction: " + trip["fave_attraction"] + "</p>";
      $("#trip-" + id).html(reviewText);
    })
  })
})
</script>

<br>
<div class="button-below-table">
  <% if @user == current_user %>
    <%= link_to "Add New Trip", new_user_trip_url(@user), :class => 'btn btn-sm btn-primary btn-rounded' %>
  <% end %>
</div>
